{
  "name": "drop-city-landing",
  "version": "1.0.0",
  "description": "Drop City — Geospatial AR Graffiti landing page",
  "private": true,
  "scripts": {
    "dev": "npx serve . -p 3333",
    "build": "echo 'Static site — no build needed'",
    "preview": "open http://localhost:3333 && npx serve . -p 3333"
  }
}
